# Compile with the latest version of c++, with warnings enables,
# and GDB support.
CPPFLAGS="-std=c++23 -Wall -Wpedantic -Wextra"

for dspp in server.cpp client.cpp; do
	"${CC:-g++}" $CPPFLAGS -o "${dspp%%.cpp}" "$dspp"
done
